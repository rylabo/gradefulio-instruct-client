# Gradeful.io Instruct

## Quick Start

First, run the development server:

```bash
npm install
npm run dev
```

Open [http://localhost:3000/homeroom/Setup](http://localhost:3000/homeroom/Setup) with your browser to see the app config UI.
